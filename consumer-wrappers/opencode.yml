name: opencode

on:
  issue_comment:
    types: [created]

permissions:
  id-token: write
  contents: write
  pull-requests: write
  issues: write

jobs:
  opencode:
    # Match /opencode but NOT /opencode-something or /opendev etc.
    if: |
      (contains(github.event.comment.body, '/opencode ') ||
       endsWith(github.event.comment.body, '/opencode')) &&
      !contains(github.event.comment.body, '/opendev') &&
      !contains(github.event.comment.body, '/openspecs') &&
      !contains(github.event.comment.body, '/openreview') &&
      !contains(github.event.comment.body, '/openrefine') &&
      !contains(github.event.comment.body, '/opendiscuss') &&
      (github.event.comment.author_association == 'tobifinn' ||
       github.event.comment.author_association == 'MEMBER')
    uses: tobifinn/opencode-workflows/.github/workflows/opencode.yml@main
    with:
      issue_number: ${{ github.event.issue.number }}
      comment_id:   ${{ github.event.comment.id }}
      comment_body: ${{ github.event.comment.body }}
      is_pr:        ${{ github.event.issue.pull_request != null }}
      test_globs:   ${{ vars.OPENCODE_TEST_GLOBS || '**/test_*.py,**/*_test.py,**/tests/**,**/*.test.ts,**/*.test.js,**/*.spec.ts,**/*.spec.js,**/*_test.go,**/*Test.java,**/*Spec.scala' }}
      code_globs:   ${{ vars.OPENCODE_CODE_GLOBS || '!**/test_*.py,!**/*_test.py,!**/tests/**,!**/*.test.ts,!**/*.test.js,!**/*.spec.ts,!**/*.spec.js,!**/*_test.go,!**/*Test.java,!**/*Spec.scala' }}
    secrets: inherit
